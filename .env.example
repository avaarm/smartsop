# OpenAI API Key (only used for initial model comparison/validation)
OPENAI_API_KEY=your_key_here

# Security Settings
JWT_SECRET=your_jwt_secret_here
ENCRYPTION_KEY=your_encryption_key_here

# Model Settings
MODEL_SAVE_DIR=./model_checkpoints
TRAINING_DATA_DIR=./collected_data

# Access Control
ALLOWED_DOMAINS=["your-company-domain.com"]
MAX_REQUESTS_PER_MINUTE=100

# Database Settings (if you decide to use a database later)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smartsop
DB_USER=your_db_user
DB_PASSWORD=your_db_password
